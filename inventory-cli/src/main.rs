use inventory::inventory::{read_inventory_file, Inventory};
use std::path::PathBuf;

fn main() {
    let path = PathBuf::from("inventory-openjdk.toml");

    //    let inventory = read_inventory_file::<String>(&path).unwrap();

    // let mut inventory = Inventory::new();
}
